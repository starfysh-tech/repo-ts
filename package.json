{
  "name": "repo-trust-extension",
  "version": "0.1.0",
  "description": "Explainable trust signals on public GitHub repository pages (MV3, Phase 1 client-only PoC).",
  "type": "module",
  "private": true,
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/starfysh-tech/repo-ts.git"
  },
  "license": "ISC",
  "dependencies": {
    "preact": "^10.29.2"
  },
  "devDependencies": {
    "@crxjs/vite-plugin": "^2.0.0-beta.33",
    "@preact/preset-vite": "^2.10.5",
    "@types/chrome": "^0.2.0",
    "typescript": "^6.0.3",
    "vite": "^8.1.0",
    "vitest": "^4.1.9"
  }
}
